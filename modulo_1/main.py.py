velocidade = float(input('Digite a velocidade do Veículo (em km/h): '))
distancia_total = float(input('Digite a distancia percorrida pelo veículo (em km): '))

velocidade_media= distancia_total/velocidade

print(f'A velocidade média é: {velocidade_media:.2f} km/h')
